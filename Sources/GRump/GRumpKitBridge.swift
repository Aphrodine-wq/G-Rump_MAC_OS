import GRumpKit

/// Compatibility alias while the GUI is progressively rewired to typed
/// GRumpKit APIs. The canonical 161 schemas now live in GRumpTools.
typealias ToolDefinitions = GRumpKit.ToolDefinitions
